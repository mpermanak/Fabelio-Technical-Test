var intA = [1,2,3,4,5],
    intB = [1,3,5,7,9];
    
function getMatch(a, b) {
   var matches = [];
    for ( var i = 0; i < a.length; i++ ) {
    for ( var e = 0; e < b.length; e++ ) {
    if ( a[i] === b[e] ) matches.Push( a[i] );
      }
    }
    return matches;
}
  getMatch(intA, intB);
